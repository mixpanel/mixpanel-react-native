struct Constants {
    static let ERROR = "ERROR:"
    static let INSTANCE_NOT_FOUND = "You have to call `getInstance(token)` before calling methods on Mixpanel Instance"
    static let LIBRARY_INVOKED = "iOS library invoked successfully"
}
